apiVersion: v1
kind: PersistentVolume
metadata:
  name: owncloud-pv
  namespace: owncloud
  labels:
    app: ownCloud
spec:
  storageClassName: manual
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/k8s"
